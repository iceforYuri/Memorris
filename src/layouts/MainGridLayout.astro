---
import Footer from '@components/Footer.astro'
import Navbar from '@components/Navbar.astro'
import BackToTop from '@components/control/BackToTop.astro'
import SideBar from '@components/widget/SideBar.astro'
import Layout from './Layout.astro'
import { Icon } from 'astro-icon/components'
import { siteConfig } from '../config'
import type { MarkdownHeading } from 'astro'
import TOC from "../components/widget/TOC.astro";
import ImageWrapper from "../components/misc/ImageWrapper.astro";
import {BANNER_HEIGHT, BANNER_HEIGHT_EXTEND, MAIN_PANEL_OVERLAPS_BANNER_HEIGHT} from "../constants/constants";

interface Props {
  title?: string
  banner?: string
  description?: string
  lang?: string
  setOGTypeArticle?: boolean;
  headings? : MarkdownHeading[]
  isPost?: boolean; // 新增属性，用于标识是否为文章页面
}

const { title, banner, description, lang, setOGTypeArticle, headings = [], isPost = false } = Astro.props
const hasBannerCredit =
  siteConfig.banner.enable && siteConfig.banner.credit.enable
const hasBannerLink = !!siteConfig.banner.credit.url

const mainPanelTop = siteConfig.banner.enable ? `calc(${BANNER_HEIGHT}vh - ${MAIN_PANEL_OVERLAPS_BANNER_HEIGHT}rem)` : "5.5rem"

// 定义侧边栏宽度为CSS变量统一控制
const sidebarWidth = "17.5rem";
---

<Layout title={title} banner={banner} description={description} lang={lang} setOGTypeArticle={setOGTypeArticle}>
<!-- ...其他代码保持不变... -->

<!-- Main content -->
<div class="absolute w-full z-30 pointer-events-none" style=`top: ${mainPanelTop}`>
    <!-- The pointer-events-none here prevent blocking the click event of the TOC -->
    <div class="relative max-w-[var(--page-width)] mx-auto pointer-events-auto">
        <!-- 根据页面类型选择不同的网格布局 -->
        <div id="main-grid" class:list={[
            "transition duration-700 w-full left-0 right-0 grid lg:grid-rows-[auto] mx-auto gap-4 px-0 md:px-4",
            // 非文章页使用原来的两栏布局
            {"grid-cols-[17.5rem_auto] grid-rows-[auto_1fr_auto]": !isPost},
            // 文章页使用新的三栏布局，并且居中
            {"grid-cols-[17.5rem_minmax(auto,_45rem)_17.5rem] grid-rows-[auto_1fr_auto] mx-auto": isPost}
        ]}>
            <!-- Banner image credit -->
            {hasBannerCredit && <a href={siteConfig.banner.credit.url} id="banner-credit" target="_blank" rel="noopener" aria-label="Visit image source"
                                   class:list={["group onload-animation transition-all absolute flex justify-center items-center rounded-full " +
                                   "px-3 right-4 -top-[3.25rem] bg-black/60 hover:bg-black/70 h-9", {"hover:pr-9 active:bg-black/80": hasBannerLink}]}
            >
                <!-- ...credit代码不变... -->
            </a>}

            <!-- 侧边栏，在文章页面时固定在第一列 -->
            <SideBar class:list={[
                "mb-4 onload-animation", 
                // 非文章页面时的布局
                {"row-start-2 row-end-3 col-span-2 lg:row-start-1 lg:row-end-2 lg:col-span-1 lg:max-w-[17.5rem]": !isPost},
                // 文章页面时的布局
                {"col-start-1 col-end-2 row-start-1 max-w-[17.5rem]": isPost}
            ]} headings={headings}></SideBar>

            <!-- 主内容区，在文章页面时固定在第二列 -->
            <main id="swup-container" class:list={[
                "transition-swup-fade overflow-hidden",
                // 非文章页面时的布局
                {"col-span-2 lg:col-span-1": !isPost},
                // 文章页面时的布局
                {"col-start-2 col-end-3": isPost}
            ]}>
                <div id="content-wrapper" class="onload-animation">
                    <slot></slot>
                    <div class="footer col-span-2 onload-animation hidden lg:block">
                        <Footer></Footer>
                    </div>
                </div>
            </main>

            <!-- 新增：文章页面时，TOC组件直接在网格中 -->
            {isPost && siteConfig.toc.enable && 
                <div id="toc-in-grid" class="col-start-3 col-end-4 row-start-1 max-w-[17.5rem] onload-animation">
                    <div class="sticky top-14 w-full h-[calc(100vh_-_20rem)] overflow-y-scroll overflow-x-hidden hide-scrollbar">
                        <div class="h-8 w-full"></div>
                        <TOC headings={headings}></TOC>
                        <div class="h-8 w-full"></div>
                    </div>
                </div>
            }

            <div class="footer col-span-2 onload-animation block lg:hidden">
                <Footer></Footer>
            </div>
        </div>

        <BackToTop></BackToTop>
    </div>
</div>

<!-- 非文章页面时，保持原来的TOC布局方式 -->
{!isPost && (
    <div class="absolute w-full z-0 hidden 2xl:block">
        <div class="relative max-w-[var(--page-width)] mx-auto">
            {siteConfig.toc.enable && <div id="toc-wrapper" class:list={["hidden lg:block transition absolute top-0 -right-[var(--toc-width)] w-[var(--toc-width)] flex items-center",
                {"toc-hide": siteConfig.banner.enable}]}
            >
                <div id="toc-inner-wrapper" class="fixed top-14 w-[var(--toc-width)] h-[calc(100vh_-_20rem)] overflow-y-scroll overflow-x-hidden hide-scrollbar">
                    <div id="toc" class="w-full h-full transition-swup-fade ">
                        <div class="h-8 w-full"></div>
                        <TOC headings={headings}></TOC>
                        <div class="h-8 w-full"></div>
                    </div>
                </div>
            </div>}

            {!siteConfig.toc.enable && <div id="toc"></div>}
        </div>
    </div>
)}
</Layout>

<style>
:root {
  --sidebar-width: 17.5rem;
  --toc-width: 17.5rem; /* 使TOC宽度与侧边栏一致 */
}
</style>